#!/usr/bin/env node

var shapefile = require('shapefile');

shapefile
  .open('../demo/shp/fujian.shp')
    .then(source => source.read()
        .then(function log(result) {
            if (result.done) return;
            console.log(result.value);
        })
    )


shapefile
